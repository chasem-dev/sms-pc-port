# Game options, one per line: name = value (on/off for switches).
# Remove the leading # to use a line. An environment variable of the same
# meaning (listed in README.md) wins over this file.

# HD texture packs from mods/textures/ (see mods/README.md): on, off, or
# folders separated by ';'.
texture_packs = on
# Video memory for texture pack images before the least used are freed, in MiB.
# texture_pack_mb = 1536

# Widescreen: off, 16:9, 21:9, 16:10 (or on for 16:9). The game sees a wider
# view; the HUD and menus stay at 4:3 in the middle.
widescreen = on
# With widescreen, where the gameplay HUD goes: centre (with the menus), or
# edges (counters to the left edge, the water gauge to the right one).
# widescreen_hud = centre

# Frame rate during gameplay: 30 (the game's own) or 60. Logos, menus and
# movies stay at 30. 60 draws twice as many frames, so it needs a GPU.
frame_rate = 60

# Game file mods from mods/<name>/files/ (see mods/README.md), as name or a;b.
# mod = none

# Internal resolution: 1 is the GameCube's 640x528, 2 twice that, and so on.
# resolution = 1
# Window size, in multiples of 640x480.
# window_scale = 1
# Wait for the display's refresh when presenting.
# vsync = off

# skip_movies = off
# audio = on
# Open the debug overlay (frame rate, where the time goes) at start.
# overlay = off

# save_dir = /path/to/memory/card/folder
# disc_image = /path/to/GMSE01.iso

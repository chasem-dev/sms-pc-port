Reason: port: 32-bit builds lay classes out as the GameCube does: g++ puts a derived class's small members in its base's tail padding (TViewObj's flags inside TNameRef, shifting every actor by 4 bytes), which CodeWarrior never does, so these bases fill theirs. Code mods written against the retail layout (SunshineHeaderInterface) need it
--- a/include/JSystem/J3D/J3DGraphBase/J3DShape.hpp
+++ b/include/JSystem/J3D/J3DGraphBase/J3DShape.hpp
@@ -43,6 +43,11 @@
 
 private:
 	u16 mUseMtxIndex;
+#if defined(TARGET_PC) && !defined(__LP64__)
+	// Port: the GameCube compiler never places a derived class's members in
+	// this class's tail padding; g++ would (Itanium ABI), so it is filled.
+	u8 _pcTailPad[2];
+#endif
 };
 
 /**
--- a/include/JSystem/JAudio/JASystem/JASInstEffect.hpp
+++ b/include/JSystem/JAudio/JASystem/JASInstEffect.hpp
@@ -19,6 +19,11 @@
 
 public:
 	/* 0x4 */ u8 mTarget;
+#if defined(TARGET_PC) && !defined(__LP64__)
+	// Port: the GameCube compiler never places a derived class's members in
+	// this class's tail padding; g++ would (Itanium ABI), so it is filled.
+	u8 _pcTailPad[3];
+#endif
 };
 
 } // namespace JASystem
--- a/include/JSystem/JDrama/JDRNameRef.hpp
+++ b/include/JSystem/JDrama/JDRNameRef.hpp
@@ -90,6 +90,11 @@
 protected:
 	/* 0x4 */ const char* mName;
 	/* 0x8 */ u16 mKeyCode;
+#if defined(TARGET_PC) && !defined(__LP64__)
+	// Port: the GameCube compiler never places a derived class's members in
+	// this class's tail padding; g++ would (Itanium ABI), so it is filled.
+	u8 _pcTailPad[2];
+#endif
 };
 
 } // namespace JDrama
--- a/include/JSystem/JDrama/JDRViewObj.hpp
+++ b/include/JSystem/JDrama/JDRViewObj.hpp
@@ -76,6 +76,11 @@
 
 public:
 	/* 0xC */ TFlagT<u16> unkC;
+#if defined(TARGET_PC) && !defined(__LP64__)
+	// Port: the GameCube compiler never places a derived class's members in
+	// this class's tail padding; g++ would (Itanium ABI), so it is filled.
+	u8 _pcTailPad[2];
+#endif
 };
 
 }; // namespace JDrama
--- a/include/Map/MapCollisionEntry.hpp
+++ b/include/Map/MapCollisionEntry.hpp
@@ -112,6 +112,11 @@
 	/* 0x20 */ Mtx unk20;
 	/* 0x50 */ JGeometry::TVec3<f32> mPrevTranslation;
 	/* 0x5C */ u16 mFlags;
+#if defined(TARGET_PC) && !defined(__LP64__)
+	// Port: the GameCube compiler never places a derived class's members in
+	// this class's tail padding; g++ would (Itanium ABI), so it is filled.
+	u8 _pcTailPad[2];
+#endif
 };
 
 class TMapCollisionStatic : public TMapCollisionBase {
--- a/include/MoveBG/MapObjBall.hpp
+++ b/include/MoveBG/MapObjBall.hpp
@@ -98,6 +98,11 @@
 	/* 0x198 */ f32 unk198;
 	/* 0x19C */ GXColorS10 unk19C;
 	/* 0x1A4 */ u8 unk1A4;
+#if defined(TARGET_PC) && !defined(__LP64__)
+	// Port: the GameCube compiler never places a derived class's members in
+	// this class's tail padding; g++ would (Itanium ABI), so it is filled.
+	u8 _pcTailPad[3];
+#endif
 };
 
 class TRandomFruit : public TResetFruit {
--- a/include/MoveBG/MapObjHide.hpp
+++ b/include/MoveBG/MapObjHide.hpp
@@ -25,6 +25,11 @@
 	/* 0x144 */ char* mHiddenShineDemoName;
 	/* 0x148 */ s32 unk148;
 	/* 0x14C */ bool mAllowReveal;
+#if defined(TARGET_PC) && !defined(__LP64__)
+	// Port: the GameCube compiler never places a derived class's members in
+	// this class's tail padding; g++ would (Itanium ABI), so it is filled.
+	u8 _pcTailPad[3];
+#endif
 };
 
 class THideObj : public THideObjBase {
--- a/include/MoveBG/MapObjRailBlock.hpp
+++ b/include/MoveBG/MapObjRailBlock.hpp
@@ -63,6 +63,11 @@
 	/* 0x148 */ s8 unk148;
 	/* 0x14A */ volatile s16 unk14A; // TODO: fake or real?
 	/* 0x14C */ s8 unk14C;
+#if defined(TARGET_PC) && !defined(__LP64__)
+	// Port: the GameCube compiler never places a derived class's members in
+	// this class's tail padding; g++ would (Itanium ABI), so it is filled.
+	u8 _pcTailPad[3];
+#endif
 };
 
 class TNormalLift : public TRailMapObj {
@@ -81,6 +86,11 @@
 	/* 0x152 */ u16 unk152;
 	/* 0x154 */ f32 unk154;
 	/* 0x158 */ s8 unk158;
+#if defined(TARGET_PC) && !defined(__LP64__)
+	// Port: the GameCube compiler never places a derived class's members in
+	// this class's tail padding; g++ would (Itanium ABI), so it is filled.
+	u8 _pcTailPad[3];
+#endif
 };
 
 class TRailBlock : public TRailMapObj {
--- a/include/MoveBG/MapObjSirena.hpp
+++ b/include/MoveBG/MapObjSirena.hpp
@@ -100,6 +100,11 @@
 	/* 0x170 */ GXColorS10 unk170[3];
 	/* 0x188 */ f32 unk188[3];
 	/* 0x194 */ bool unk194;
+#if defined(TARGET_PC) && !defined(__LP64__)
+	// Port: the GameCube compiler never places a derived class's members in
+	// this class's tail padding; g++ would (Itanium ABI), so it is filled.
+	u8 _pcTailPad[3];
+#endif
 };
 
 class TItemSlotDrum : public TSlotDrum {
--- a/include/MoveBG/MapObjTown.hpp
+++ b/include/MoveBG/MapObjTown.hpp
@@ -81,6 +81,11 @@
 
 public:
 	/* 0x138 */ u16 unk138;
+#if defined(TARGET_PC) && !defined(__LP64__)
+	// Port: the GameCube compiler never places a derived class's members in
+	// this class's tail padding; g++ would (Itanium ABI), so it is filled.
+	u8 _pcTailPad[2];
+#endif
 };
 
 class TMapObjChangeStageHipDrop : public TMapObjChangeStage {

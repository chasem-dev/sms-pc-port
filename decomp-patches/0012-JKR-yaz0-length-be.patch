Reason: endian: read the Yaz0 header's decompressed length as big-endian
--- a/src/JSystem/JKernel/JKRDvdRipper.cpp
+++ b/src/JSystem/JKernel/JKRDvdRipper.cpp
@@ -319,7 +319,7 @@
 		return -1;
 	}
 
-	endPtr = dest + (((SYaz0Header*)src)->length - fileOffset);
+	endPtr = dest + (port_be32(&((SYaz0Header*)src)->length) - fileOffset);
 	if (endPtr > dest + maxDest) {
 		endPtr = dest + maxDest;
 	}
--- a/src/JSystem/JKernel/JKRAram.cpp
+++ b/src/JSystem/JKernel/JKRAram.cpp
@@ -387,7 +387,7 @@
 		return -1;
 	}
 
-	u8* endPtr = dest + (((SYaz0Header*)src)->length - fileOffset);
+	u8* endPtr = dest + (port_be32(&((SYaz0Header*)src)->length) - fileOffset);
 	if (endPtr > dest + maxDest) {
 		endPtr = dest + maxDest;
 	}
--- a/src/JSystem/JKernel/JKRDvdAramRipper.cpp
+++ b/src/JSystem/JKernel/JKRDvdAramRipper.cpp
@@ -328,7 +328,7 @@
 		return -1;
 	}
 
-	endAddr = dest + (((SYaz0Header*)src)->length - fileOffset);
+	endAddr = dest + (port_be32(&((SYaz0Header*)src)->length) - fileOffset);
 	if (endAddr > dest + maxDest) {
 		endAddr = dest + maxDest;
 	}
